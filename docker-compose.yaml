services:
  app:
    container_name: jiosaavn-api
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
